# Codex skills

Canonical workspace for personal Codex skills. Skill sources live in `skills/`; the local install discovers them through individual links in `~/.agents/skills`.

See [skill authoring](docs/skill-authoring.md) for the required authoring workflow. Regression cases that are not part of a runtime skill live in `evaluations/`.
